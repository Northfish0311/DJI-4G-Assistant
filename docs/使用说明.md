# DJI 4G Assistant（大疆 4G 助手）中文使用说明

DJI 4G Assistant（大疆 4G 助手）是一个 Windows 桌面程序，把模块诊断、网络状态、eSIM、短信、电话、USSD、AT 工具和原始模块初始化放在同一个界面里。

## 第一次使用

1. 打开 [GitHub Releases](https://github.com/Northfish0311/DJI-4G-Assistant/releases)，找到最上方带 **Latest** 标记的版本。
2. 下载 `DJI-4G-Assistant-Setup-版本号-x64.exe`。普通用户只需要这个文件。
3. 双击安装包，按提示完成安装。
4. 把 SIM/eSIM 卡装进模块，再把模块插入 Windows 电脑。
5. 双击桌面上的 **DJI 4G Assistant** 图标。
6. 等待程序自动发现 AT 口并读取设备状态。只有需要重新检查时才点击右上角“自动扫描”。

正常情况下不用查 COM 号、不用手工填写 `COM5`，也不用先打开黑色命令窗口。安装版和便携版双击后都会直接打开管理界面。

安装版会创建桌面和开始菜单快捷方式。项目目前没有购买商业代码签名证书，因此 Edge 下载和 Windows 首次安装时可能显示安全提醒。

### Edge 下载时提示不安全怎么办

1. 先确认浏览器地址是 `github.com/Northfish0311/DJI-4G-Assistant`，下载文件名以 `DJI-4G-Assistant-Setup-` 开头并以 `-x64.exe` 结尾。
2. Edge 显示“此文件不常下载”或类似提示时，打开该下载项右侧菜单，选择“保留”。
3. 如果 Edge 再次询问，选择“显示详细信息”或“仍然保留”。
4. 不要关闭 Edge 的整体安全功能，也不要从网盘、聊天附件或陌生镜像下载本软件。

### 安装时 Windows 再次提醒怎么办

1. 双击安装包后，如果出现“Windows 已保护你的电脑”，先确认文件名仍然正确。
2. 点击“更多信息”。
3. 点击“仍要运行”，然后按安装向导继续。
4. 如果下载地址不是本仓库、文件名异常，或 SHA256 与 `SHA256SUMS.txt` 不一致，请停止安装并删除文件。

这是未购买商业代码签名证书造成的信誉提醒，不代表可以忽略所有安全警告。只对本仓库 Releases 中、校验一致的安装包执行以上步骤。

### Releases 里的文件怎么选

| 文件 | 是什么 | 怎么用 |
| --- | --- | --- |
| `DJI-4G-Assistant-Setup-版本号-x64.exe` | 正式安装版 | **普通用户下载并安装这个** |
| `DJI-4G-Assistant-Portable-版本号-x64.exe` | 免安装便携版 | 不想安装时可以直接双击运行 |
| `.exe.blockmap`（旧版发布页可能出现） | 打包工具生成的数据文件 | 不用下载；新版不再发布 |
| `SHA256SUMS.txt` | 文件校验值 | 需要验证下载文件时使用 |
| `Source code (zip)` / `Source code (tar.gz)` | GitHub 自动生成的开发者源码 | 普通用户不用下载，不能当作软件运行 |

两个 EXE 都包含完整程序，所以体积接近。安装版和便携版功能相同，区别只是是否安装、是否创建快捷方式。旧版本会保留作为回退备份；普通用户始终使用最上方带 **Latest** 标记的版本。

## 六个常用操作

### 1. 检查模块能不能上网

1. 插好 SIM/eSIM 和模块，打开软件。
2. 等待软件自动读取核心状态；需要完整复查时再点击右上角“自动扫描”，等待检查结束。
3. 在“概览”确认 SIM 为 `READY`、网络为“本地注册”或“漫游注册”，并显示运营商、信号和模块 IP。
4. 打开“网络”，确认 Quectel ECM 网卡已连接，并获得 IPv4、网关和 DHCP。

蜂窝已经注册但 Windows 网卡未连接时，只检查“网络”页和驱动，不要重新写 eSIM、VID/PID 或 APN。

### 2. 读取和发送短信

1. 打开“短信”页。
2. 点击“刷新”读取模块存储中的短信；验证码会单独显示并可复制。
3. 发短信时填写收件号码和正文，点击“发送短信”。
4. 核对号码并确认。软件显示“模块已接受”后，是否最终送达仍由运营商和套餐决定。

### 3. 切换已有 eSIM 套餐

1. 打开“eSIM”页，点击“刷新”。
2. 找到目标套餐，点击“切换到此套餐”。
3. 确认后不要拔卡、拔模块或关闭软件。
4. 等待 30–60 秒，软件会自动重新读取状态；如果页面没有及时更新，再点击“自动扫描”确认新运营商已经注册。

当前使用的套餐会标记为“正在使用”。如果只显示一张，先核对页面 EID，再点“检查待下载”；套餐商 App 里的订单不一定已经写入这张卡。

### 4. 下载新的 eSIM 套餐

1. 向套餐商取得你自己购买套餐的完整 `LPA:1$...` 激活码。
2. 打开“eSIM”，展开“下载新套餐”。
3. 粘贴完整激活码，点击“下载”并确认。
4. 完成后刷新列表，再选择是否启用新套餐。

下载只会新增 Profile，不会自动删除已有套餐。激活码不会保存到项目或浏览器本地存储。

### 5. 初始化刚买来的原始模块

程序启动后会先自动扫描。只有程序确认设备属于已验证的 Baiwang/QDC507 风格原始模块，并读取到真实配置时，才使用“原始模块设置”。

1. 先点“检查原始 USB”，只读核对 VID/PID、型号、固件和 `usbcfg`。
2. 按页面要求输入确认词，完成 VID/PID 转换。
3. 等模块重新出现后，再单独确认 `usbnet=1`。
4. 重新插拔并自动扫描，确认 COM 口和 ECM 网卡都出现。

如果模块已经是 `2C7C:0125 + usbnet=1` 且能联网，不要执行初始化。

### 6. 接打电话

1. 先确认 SIM/eSIM 套餐支持语音或 VoLTE，再打开“电话”页；软件会自动监听来电并检查模块能力。
2. 拨出时输入真实号码并点一次“拨打”；来电时点“接听”。这些日常操作不再弹出第二个确认框，执行结果会直接显示在拨号盘下方。
3. 通话接通后，如果页面自动发现模块的标准 USB 音频设备，“高级信息”中会出现“启动声音”，可使用电脑麦克风和扬声器通话。
4. 挂断后音频桥会自动停止并释放麦克风，也可以在“高级信息”中手动停止。

能拨号只代表 AT 呼叫控制可用，不保证一定有声音。部分 QDC507 定制固件只暴露 PCM 能力，没有 Windows 可用的标准 USB 音频端点；本软件不会为了声音自动修改模块配置。

## 页面功能

### 总览
显示 USB 身份、AT 端口、SIM、运营商、信号、注册状态、无线制式、APN、PDP 和模块 IP。程序启动时自动读取核心状态，“自动扫描”只读取信息并重新检查，不会修改模块。

### 网络
显示 Windows 网络适配器、连接状态、IPv4、网关、DHCP、驱动版本和本次运行的收发量。少数 ECM 驱动不提供可信流量计数，软件会显示 `--`，不会把异常计数当成真实流量。

“安装或修复官方驱动”只针对精确识别为 `USB\VID_2C7C&PID_0125&MI_04` 的 Windows ECM 接口。点击后输入 `ECMDRIVER`，再通过 Windows 管理员授权。程序从 Quectel 官方地址临时下载驱动，校验固定 SHA256、硬件 ID、版本、DHCP 配置和数字签名，备份当前 Quectel 驱动后再安装。仓库和 EXE 不内置驱动文件。

驱动已就绪时不会重复修改。目标接口不存在、下载内容变化、签名无效或管理员授权取消时会停止。这个操作只修复 Windows 驱动，不会修改模块的 AT、APN、SIM、eSIM Profile、VID/PID 或 `usbnet`。

切换 USB 网卡模式会写入并重启模块，只能用于已验证设备；已经正常联网的模块不要切换。

### eSIM
程序通过 lpac 读取当前 eUICC 的标准 Profile 列表，可启用、停用、改昵称、用合法的完整 `LPA:1$...` 激活码下载、处理通知，并在双重确认后删除未启用 Profile。写入时不要拔卡、拔模块或关闭程序。

页面顶部把三个容易混淆的概念分开显示：

- **EID**：当前这张 eUICC 的唯一编号。
- **卡内套餐**：当前 EID 中已经写入、且标准接口可读取的已启用和已停用 Profile。
- **待下载套餐**：GSMA SM-DS 中等待这张卡领取的 Profile，需要点击“检查待下载”。

套餐商 App 里的订单数量不等于卡内 Profile 数。若你预期三张却只看到一张，先比较 App 中的 EID；另外两份可能尚未下载、写在另一张卡、只存在于套餐商账户，或属于厂商未按标准 Profile 暴露的 bootstrap 身份。当前实测中，lpac 的原始列表与网页都只返回同一张卡内 Profile，网页没有过滤隐藏项。

eUICC 通常不保存套餐剩余流量。界面中的流量是 Windows 网卡实际收发量，不是套餐余额；准确余额需要套餐商官方 API。

### 短信
收件箱按消息卡片显示号码、时间和正文，并识别常见 4–8 位验证码。发送使用 UCS2/PDU，支持中文和长短信分段。发送成功只代表模块接受请求，不代表对方已收到；漫游、短信中心、套餐权限和运营商风控均会影响送达。

### 电话
可监控来电、拨号、接听、挂断、读取来电号码并发送 DTMF。电话控制使用标准 AT 指令，并与现有数据连接分开识别，模块的 LTE 数据会话不会被误判为语音通话。

进入“电话”页后，程序会自动只读检查模块能力和 Windows 音频端点，结果放在默认折叠的“高级信息”中。只有同时检测到模块的 USB 音频输入、USB 音频输出，并且语音通话已经接通，Windows 本机才会开放“启动声音”。音频桥把模块下行语音送到电脑扬声器，把电脑麦克风送回模块；局域网手机和平板页面不能代替 Windows 完成这条音频链路。

电话是否能建立取决于 SIM/eSIM、套餐、运营商 VoLTE/2G/3G 语音回落和模块固件。程序不会自动修改 `usbnet`、`usbcfg`、APN、eSIM Profile，也不会自动启用 ADB/UAC 或安装第三方内核模块。

### USSD
可发送运营商服务代码。并非所有漫游卡、数据卡或网络支持 USSD。

### AT 工具
用于读取模块和排错。危险写入默认拦截，不要执行来源不明的 `QCFG`、`CFUN`、`CGDCONT` 等命令。

### 系统
显示自动发现的 AT 端口、局域网地址和本次启动的临时密码。程序自动从 `8787` 开始寻找空闲网页端口；AT 端口输入框保持“自动”即可，不需要固定写成 `COM5`。

## 用手机或平板打开

1. 让移动设备和 Windows 电脑连接同一可信 Wi-Fi。
2. 打开“系统”页显示的 `http://192.168.x.x:端口`。
3. 输入同页显示的临时控制台密码。

`127.0.0.1` 只代表当前设备自己，手机不能使用电脑的 `127.0.0.1`。不要把端口映射到公网。

## iPad、iPhone 和手机浏览器

- **把模块直接插到 iPad/iPhone 上网：** 这是模块自己的 ECM 网卡能力，不需要 Windows 软件。只要接口、供电和系统版本原来能识别，打开本软件或执行“自动扫描”不会影响它。
- **把模块直接插到 iPad 管理 AT/eSIM/短信/电话：** 当前 Windows 程序做不到，需要另做带 USB DriverKit 扩展、Apple 签名和专用音频链路的原生 iPadOS App，不能把现有网页直接打包后冒充支持。
- **用手机/平板管理插在 Windows 上的模块：** Windows 软件必须保持运行，手机和电脑连接同一可信 Wi-Fi，再打开“系统”页给出的 `192.168.x.x:端口` 地址并输入临时密码。
- 手机上的 `127.0.0.1` 代表手机自己，不能用来访问 Windows 电脑。
- iPhone/iPad 直连能否识别还取决于 USB-C/转接器供电；这不是 eSIM Profile 是否兼容的问题。

## 新模块初始化

仅当程序确认是已验证的 Baiwang/QDC507 风格原始设备时使用。已成功联网的模块不要执行。

1. 运行只读检查。
2. 核对 VID/PID、`ATI`、固件和设备自己的 `usbcfg`。
3. 确认后保存本机基线，只改 VID/PID。
4. 重启并重新识别后，再单独确认 `usbnet=1`。
5. 再次插拔并扫描，确认 AT 口和 Windows 网卡都出现。

`2CA3:4006` 只是已观察到的一种身份，不代表所有 DJI 配件都相同。程序不会仅凭名称套用固定命令。

## Windows 原生与 VoHive 怎么选

| 目标 | 使用模式 | 结果 |
| --- | --- | --- |
| 在 Windows 上网、收发短信、管理 eSIM | `usbnet=1`（ECM） | Windows 看到 Quectel 有线网卡；这是本软件的默认路线 |
| 在 Linux/VoHive 中做多设备、代理或 VoWiFi 实验 | `usbnet=0`（QMI） | Linux 使用 `qmi_wwan` / `cdc-wdm`；Windows ECM 会暂时消失 |

两种模式不能同时占用。切换后模块会重启并重新枚举，COM 号也可能变化。已经在 Windows 正常上网时，不要为了“试试”切 QMI。

诊断请依次确认：

1. USB：Windows 能看到设备和真实 VID/PID。
2. 串口：出现 Quectel AT 口，程序能收到 `OK`。
3. SIM：`CPIN: READY`。
4. 蜂窝：`CEREG` 已注册，并能读取运营商与 LTE 频段。
5. 数据：PDP 已激活，模块获得 IP。
6. Windows：ECM 网卡启用、DHCP 正常，并获得 IPv4 与网关。

只有卡在哪一层，才修哪一层。不要把驱动故障当成 SIM 故障，也不要因为 Windows 没拿到地址就重新写 eSIM 或 VID/PID。

## 异常设备救援

其他工具改动后若 COM 或网卡消失，先做只读救援诊断。它检查 USB、Windows 驱动、COM、网卡、AT 口和原始接口，并保存可读取基线。完全找不到 AT 口时先检查数据线、供电、USB 和驱动；没有读取真实配置前不要盲写参数。

## 常见问题

**扫描不到模块：** 更换支持数据的 USB 线和接口，等待 Windows 驱动完成后重扫。

**LTE 已注册但 Windows 没网：** 先在“网络”页查看驱动、DHCP、IPv4 和网关。若设备是精确匹配的 `2C7C:0125 / MI_04`，且页面提示驱动需要修复，可使用“安装或修复官方驱动”。驱动正常但仍没网时，再检查 `usbnet`、PDP 和 APN；不要重复初始化模块。

**看不到多张 eSIM：** 先在 eSIM 页查看 EID 和“卡内套餐”，再点“检查待下载”。标准列表会同时包含已启用和已停用 Profile；套餐商 App 中的订单、另一 EID 的套餐或非标准 bootstrap 身份不会自动出现在这里。

**短信发出但对方未收到：** 检查短信中心、国际漫游和套餐是否支持主叫短信，用已知可发短信的实体 SIM 对照。

**电话能拨号但没有声音：** 打开电话页的“高级信息”查看自动检查结果。如果显示“没有 USB 声卡”或“只有 PCM 接口”，说明模块没有向 Windows 暴露可桥接的标准音频输入和输出；这不是网页音量问题。不要为了试声音盲改 `usbcfg` 或加载来源不明的驱动。若显示音频设备且通话已接通，再点“启动声音”，并允许 Windows 麦克风权限。

**电话点了没反应：** 软件会自动寻找 AT 口。请先输入真实电话号码，不要把输入框提示文字当成号码；拨号结果会立即显示在拨号盘下方。接听只在来电状态开放，按键音只在已接通时开放，启动声音还要求页面检测到标准 USB 音频端点。

**套餐余额不显示：** 这是标准字段限制，需要套餐商 API。

## 桌面版和源码版的区别

普通用户请运行 Releases 中的 Windows EXE。拨号、接听、挂断和只读刷新等日常操作直接执行；eSIM 删除、USB 模式、原始模块转换等高风险操作仍会要求确认。

开发者直接运行 `npm start` 时默认是只读服务，因此切换套餐、下载套餐、发送短信和电话控制按钮可能处于锁定状态。这是安全保护，不是功能缺失。双击源码中的 `Start-Web-Console.cmd` 会开放完整本地控制台，并为本次启动自动生成带密码的本机和局域网地址；电话等日常操作直接执行，高风险操作仍需确认，关闭窗口后临时密码失效。

## 开发者从源码运行

安装 [Node.js LTS](https://nodejs.org/) 后：

```powershell
npm install
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\install-lpac.ps1
npm test
npm run desktop
```

普通用户应使用桌面 EXE。
